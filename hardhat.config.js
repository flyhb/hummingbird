require("@nomicfoundation/hardhat-toolbox");

/**
 * @type import('hardhat/config').HardhatUserConfig
 */
module.exports = {
  solidity: "0.8.19",
  networks: {
    // Default Hardhat network configuration.  You can add custom networks
    // (e.g. goerli, mainnet) in your own environment files.
    hardhat: {},
  },
};